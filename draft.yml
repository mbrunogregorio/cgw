models:
  Models\Slide:
    name: string
    link: string nullable
    description: string nullable
    image: string nullable
    timestamps: true
    softDeletes: true

  Models\Banner:
    name: string
    image: string nullable
    link: string nullable
    embed: mediumText nullable
    timestamps: true
    softDeletes: true

  Models\Tip:
    title: string
    description: string
    timestamps: true
    softDeletes: true

seeders: Slide, Banner, Tip
